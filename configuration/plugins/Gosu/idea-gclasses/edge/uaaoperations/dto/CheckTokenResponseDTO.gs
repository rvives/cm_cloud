package edge.uaaoperations.dto

class CheckTokenResponseDTO {
  //potential additions to this DTO include: user_id, user_name, email.
  //None are needed so none are added. An invalid/expired/revoked token returns a 400 error
}
