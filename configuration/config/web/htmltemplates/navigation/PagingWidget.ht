<div id="p--prev--${focusIdRoot}" class="gw-Paging--prev" role="button" aria-label="${prevLabel}" aria-disabled="${!prevPage ? 'true' : 'false'}" data-gw-click="?{prevPage} gwPaging.page id:^ page:${prevPage}"><div class="gw-icon"></div></div>
<div id="p--next--${focusIdRoot}" class="gw-Paging--next" role="button" aria-label="${nextLabel}" aria-disabled="${!nextPage ? 'true' : 'false'}" data-gw-click="?{nextPage} gwPaging.page id:^ page:${nextPage}"><div class="gw-icon"></div></div>
<div :if="isHierarchical" id="p--parent--${focusIdRoot}" class="gw-Paging--parent" role="button" aria-label="${parentLabel}" aria-disabled="${!parentPage ? 'true' : 'false'}" data-gw-click="?{parentPage} gwPaging.parent id:^"><div class="gw-icon"></div></div>
<div :if="isHierarchical" id="p--child--${focusIdRoot}" class="gw-Paging--child" role="button" aria-label="${childLabel}" aria-disabled="${!childPage ? 'true' : 'false'}" data-gw-click="?{childPage} gwPaging.child id:^"><div class="gw-icon"></div></div>
