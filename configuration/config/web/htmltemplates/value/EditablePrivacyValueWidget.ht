<input type="text" name="${renderId}" value="${currentValue}" disabled="${disabled || encrypted}" aria-required="${required ? 'true' : 'false'}"
       placeholder="${placeholder}" class="gw-PrivacyValueWidget--valueField ${noTrack ? 'gw-noTrack'}"/>
