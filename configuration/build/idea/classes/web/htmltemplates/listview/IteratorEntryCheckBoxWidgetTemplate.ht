<input type="checkbox" value="true" name="${renderId}"
       data-gw-flags-scope="${scope}" data-gw-flags-for-entry="${flags}" data-gw-flags-target="${target}"
       checked="${checked}" aria-label="${ariaLabel}"/>
