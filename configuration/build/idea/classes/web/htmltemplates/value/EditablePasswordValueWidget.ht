<input name="${renderId}" type="password"
       class="gw-PasswordValueWidget--valueField gw-noTrack" disabled="${disabled}" aria-required="${required ? 'true' : 'false'}"
       value="${currentValue}" placeholder="${placeholder}"/>
