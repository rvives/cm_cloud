<label :wrapIf="label">
  <input type="checkbox" disabled="${disabled}" aria-required="${required ? 'true' : 'false'}" class="${noTrack ? 'gw-noTrack'}" name="${renderId}" checked="${checked}"/>
  <span :if="label" class="gw-label--inner">${label}</span>
</label>
